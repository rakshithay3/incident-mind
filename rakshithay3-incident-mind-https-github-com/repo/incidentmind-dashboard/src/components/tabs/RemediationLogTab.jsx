// Wireframe only — becomes the agent dispatch trace once Dharunya's
// Report Agent JSON schema is finalized (Weeks 6-8 sync point).

export default function RemediationLogTab() {
  return (
    <div className="tab-panel">
      <div className="wireframe-note">
        Wireframe — will list agent dispatch trace: which agent, which service, what it found.
      </div>
      <div className="wireframe-box">
        <span className="wireframe-box-label">[ agent dispatch trace placeholder ]</span>
      </div>
    </div>
  )
}
